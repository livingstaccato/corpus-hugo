---
title: Home
---

This is a **separate Hugo site** — its own `hugo.toml`, its own build — that
shares the main exampleSite's `contexts.json`. Use the switcher next to the
title, or a footer pill, to jump between the two. Search here also spans
both corpora.
